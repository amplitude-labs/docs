export const ApiExamples = () => (
 <CodeGroup>
  ```javascript api.js
  const response = await fetch('https://api.justextract.it/v1/extract', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': 'Bearer YOUR_API_KEY'
    },
    body: JSON.stringify({
      url: 'https://example.com/article',
      options: {
        // Optional parameters
        includeImages: true,
        includeLinks: true,
        maxLength: 1000
      }
    })
  });

const data = await response.json();

````

```python api.py
import requests

response = requests.post(
    'https://api.justextract.it/v1/extract',
    headers={
        'Content-Type': 'application/json',
        'Authorization': 'Bearer YOUR_API_KEY'
    },
    json={
        'url': 'https://example.com/article',
        'options': {
            'includeImages': True,
            'includeLinks': True,
            'maxLength': 1000
        }
    }
)

data = response.json()
````

```ruby api.rb
require 'net/http'
require 'json'

uri = URI('https://api.justextract.it/v1/extract')
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

request = Net::HTTP::Post.new(uri)
request['Content-Type'] = 'application/json'
request['Authorization'] = 'Bearer YOUR_API_KEY'
request.body = {
  url: 'https://example.com/article',
  options: {
    includeImages: true,
    includeLinks: true,
    maxLength: 1000
  }
}.to_json

response = http.request(request)
data = JSON.parse(response.body)
```

```php api.php
$ch = curl_init('https://api.justextract.it/v1/extract');
$data = [
    'url' => 'https://example.com/article',
    'options' => [
        'includeImages' => true,
        'includeLinks' => true,
        'maxLength' => 1000
    ]
];

curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Content-Type: application/json',
    'Authorization: Bearer YOUR_API_KEY'
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
```

```go api.go
package main

import (
    "bytes"
    "encoding/json"
    "net/http"
)

type RequestBody struct {
    URL     string                 \`json:"url"\`
    Options map[string]interface{} \`json:"options"\`
}

func main() {
    requestBody := RequestBody{
        URL: "https://example.com/article",
        Options: map[string]interface{}{
            "includeImages": true,
            "includeLinks":  true,
            "maxLength":    1000,
        },
    }

    jsonData, _ := json.Marshal(requestBody)
    req, _ := http.NewRequest("POST", "https://api.justextract.it/v1/extract", bytes.NewBuffer(jsonData))
    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
}
```

</CodeGroup>
);
